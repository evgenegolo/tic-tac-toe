import { StyleSheet } from 'react-native';


const styles = StyleSheet.create({
    ScoreCounterRootActive: {
        flex: 1,
        alignItems: 'center',
        justifyContent: 'flex-end',
        backgroundColor: "#edb4b4"
    },
    ScoreCounterRootNotActive: {
        flex: 1,
        alignItems: 'center',
        justifyContent: 'flex-end',
    },
    Text: {
        fontSize: 32,
        fontFamily: "serif",
    },
    PlayerText: {
        color: "#25803c",
    }
});

export default styles;